# Task Manager - Система управління завданнями

Навчальний проект системи управління завданнями, реалізований на чистому PHP 8.3+ з використанням PostgreSQL та Active Record pattern.

## 🚀 Технології

- **PHP 8.3+** - мова програмування
- **PostgreSQL** - база даних
- **Composer** - менеджер залежностей
- **Bootstrap 5** - UI framework
- **ramsey/uuid** - генерація UUID
- **vlucas/phpdotenv** - управління змінними оточення

## 📋 Вимоги

- PHP 8.3 або вище
- PostgreSQL 12+
- Composer
- Розширення PHP: `pdo`, `pdo_pgsql`

## 🔧 Встановлення

### 1. Клонування репозиторію
```bash
git clone <repository-url>
cd task-manager
```

### 2. Встановлення залежностей
```bash
composer install
```

### 3. Налаштування .env файлу

Створіть файл `.env` в корені проекту:
```env
DB_HOST=localhost
DB_PORT=5432
DB_NAME=task_manager
DB_USER=postgres
DB_PASSWORD=your_password
```

### 4. Створення бази даних

**Linux/macOS (через sudo):**
```bash
sudo -u postgres psql -c "CREATE DATABASE task_manager;"
sudo -u postgres psql -d task_manager -f database/schema.sql
```

**Або через пароль:**
```bash
psql -U postgres -c "CREATE DATABASE task_manager;"
psql -U postgres -d task_manager -f database/schema.sql
```

## ▶️ Запуск проекту

Запустіть вбудований PHP сервер:
```bash
php -S localhost:8000 -t public/ public/router.php
```

Відкрийте браузер: [http://localhost:8000](http://localhost:8000)

## 📁 Структура проекту
```
task-manager/
├── composer.json          # Залежності проекту
├── .env                   # Конфігурація (не в git)
├── database/
│   └── schema.sql        # SQL схема бази даних
├── src/
│   ├── Database/
│   │   └── Database.php  # Singleton PDO підключення
│   ├── Models/
│   │   ├── User.php      # Модель користувача
│   │   ├── Task.php      # Модель завдання
│   │   └── TaskManager.php # Модель зв'язків
│   └── Enums/
│       └── Status.php    # Enum статусів завдань
├── public/
│   ├── index.php         # Головний роутер
│   ├── router.php        # Роутер для PHP dev server
│   ├── bootstrap.php     # Ініціалізація (autoload, .env)
│   └── views/
│       ├── home.php      # Головна сторінка
│       ├── login.php     # Сторінка входу
│       ├── register.php  # Реєстрація
│       ├── tasks.php     # Управління завданнями
│       └── 404.php       # Сторінка помилки
└── vendor/               # Composer залежності
```

## 🎯 Функціонал

### Користувачі
- ✅ Реєстрація нового користувача
- ✅ Вхід в систему (з хешуванням паролів)
- ✅ Зміна пароля
- ✅ Вихід з системи

### Завдання
- ✅ Створення нового завдання
- ✅ Перегляд списку завдань
- ✅ Зміна статусу завдання
- ✅ Видалення завдання
- ✅ Призначення завдання користувачу

### Статуси завдань
- `NOT_COMPLETED` - Не виконано
- `COMPLETED` - Виконано
- `ACCEPT` - Прийнято
- `NOT_ACCEPT` - Не прийнято

## 🔐 Безпека

- Паролі зберігаються з використанням `password_hash()` (bcrypt)
- Prepared statements для захисту від SQL-ін'єкцій
- XSS захист через `htmlspecialchars()`
- Перевірка авторизації на захищених сторінках

## 🛠 Використані PHP фічі (8.3+)

- ✅ `declare(strict_types=1)`
- ✅ Typed properties
- ✅ Named arguments
- ✅ Default parameters
- ✅ Match expressions
- ✅ Enums (backed)
- ✅ Nullsafe operator (`?->`)
- ✅ Arrow functions
- ✅ Constructor property promotion

## 📝 Архітектурні патерни

- **Active Record** - моделі містять логіку роботи з БД
- **Singleton** - єдине підключення до БД
- **MVC** (спрощений) - розділення логіки та представлення

## 🐛 Troubleshooting

### Помилка: "could not find driver"
Встановіть розширення PostgreSQL для PHP:
```bash
# Fedora/RHEL
sudo dnf install php-pgsql

# Ubuntu/Debian
sudo apt install php-pgsql
```

Перезапустіть PHP сервер після встановлення.

### Помилка: "password authentication failed"
На Linux використовуйте `sudo -u postgres` для підключення до PostgreSQL.

## 📚 Навчальні цілі

Цей проект демонструє:
- Роботу з PDO та PostgreSQL
- Реалізацію Active Record pattern
- Використання PHP 8.3+ фіч
- Організацію MVC структури без фреймворків
- Роботу з Composer та autoloading (PSR-4)
- Безпечну роботу з паролями та даними користувачів

## 📄 Ліцензія

Навчальний проект для освітніх цілей.